<%= render :partial => "samples/seq_density", :locals => {
    :sample => @chip_chip,
    :bioentry => @bioentry,
    :data => @chip_chip.density_chart_summary( :bioentry => @bioentry, :density => (params[:density]||500).to_i )
  }
%>
