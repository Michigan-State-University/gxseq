<%=
{ :success => true,
  :data => {
     :institution => {
        :name => "GLBRC",
        :url => "http:\/\/www.glbrc.org\/",
        :logo => ""
     },
     :engineer => {
        :name => "#{APP_CONFIG[:admin_email]}",
        :email => "#{APP_CONFIG[:admin_eamil]}"
     },
     :service => {
        :title => "#{track.class.name}",
        :description => ""
     },
     :text => "#{render :partial => "synthetics/sample_info"}"
  }
}.to_json.html_safe
%>