<%= render :partial => "samples/seq_density", :locals => {
    :sample => @re_seq,
    :bioentry => @bioentry,
    :data => @re_seq.density_chart_summary( :bioentry => @bioentry, :density => (params[:density]||500).to_i )
  }
%>