<%= render :partial => "samples/seq_density", :locals => {
    :sample => @chip_seq,
    :bioentry => @bioentry,
    :data => @chip_seq.density_chart_summary( :bioentry => @bioentry, :density => (params[:density]||500).to_i )
  }
%>