<% content_for :top_bar do %>
	<!-- Header here -->
	<h1 class="pagetitle">Edit Taxonomy Version: <%= @taxon_version.id %></h1>
<% end %>

<% content_for :item_bar do %>
	<!-- Filter / Search items here -->
	<div style="float:left">
	</div>
<% end %>

<% content_for :sidebar do %>
	<!-- Sidebar / Links / Notices here -->
	<%= render :partial => "bioentries/sidebar", :locals => {:active => 'taxon'} %>
<% end %>

<% if @taxon_version.nil? %>
  Taxon Version Not found...
<% else %>
  <% form_for @taxon_version do |f| -%>
    <%= f.error_messages %>
    <p>Species:<br/><%= @taxon_version.species.scientific_name.name %></p>
    <p>Taxon:<br/><%= @taxon_version.taxon.scientific_name.name %></p>

    <p><%= f.label 'version' %><br/>
    <%= f.text_field :version %></p>
  
    <%= f.submit %> |
    <%= link_to 'Cancel',:back %>
  <% end %>
<% end %>